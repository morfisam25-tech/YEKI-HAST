# W25 Owner Internal Beta Preview contract.
# VERCEL_ENV and NODE_ENV are platform/runtime values and are not copied from Production.
# Values for the names below must be Preview-only. The workflow remains disabled until provisioned.
INTERNAL_BETA_OWNER_TEST_MODE
INTERNAL_BETA_DATABASE_URL
INTERNAL_BETA_OWNER_TEST_AUTH_TOKEN
