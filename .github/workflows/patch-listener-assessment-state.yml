name: One-shot Listener assessment state hardening

on:
  push:
    branches:
      - feat/v1-2-internet-voice-foundation
    paths:
      - .github/workflows/patch-listener-assessment-state.yml
      - .github/scripts/patch-listener-assessment-state.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: feat/v1-2-internet-voice-foundation
          fetch-depth: 0

      - name: Apply Listener state patch
        run: python .github/scripts/patch-listener-assessment-state.py

      - name: Run focused guard test
        run: node --experimental-strip-types --test tests/listener-assessment-state-guard.test.ts

      - name: Commit patch and remove temporary files
        shell: bash
        run: |
          rm .github/workflows/patch-listener-assessment-state.yml
          rm .github/scripts/patch-listener-assessment-state.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add services/api/src/routes/listener.ts apps/web/app/listener/page.tsx tests/listener-assessment-state-guard.test.ts .github/workflows/patch-listener-assessment-state.yml .github/scripts/patch-listener-assessment-state.py
          git diff --cached --check
          git commit -m "fix: harden Listener assessment state transitions"
          git push origin HEAD:feat/v1-2-internet-voice-foundation