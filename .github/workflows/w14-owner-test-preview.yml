name: W14 Owner Test Preview (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Security quarantine
        run: |
          echo "W14 Owner Test Preview workflow disabled after W24 security audit."
          echo "Do not reconnect Internal Beta Preview to Production database or Production runtime secrets."
          echo "A replacement workflow must use an isolated non-Production database, an allow-listed Preview-only env, and explicit Owner Test authorization."
          exit 1
