{
  "name": "@yeki-hast/mobile",
  "private": true,
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "generate:artwork": "node scripts/generate-artwork.mjs",
    "eas-build-pre-install": "node scripts/generate-artwork.mjs",
    "export:android": "npm run generate:artwork && expo export --platform android --output-dir dist-android --clear",
    "export:ios": "npm run generate:artwork && expo export --platform ios --output-dir dist-ios --clear",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "expo": "~57.0.9",
    "expo-secure-store": "~57.0.2",
    "expo-status-bar": "~57.0.0",
    "react": "19.2.3",
    "react-native": "0.86.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "typescript": "~6.0.3"
  },
  "engines": {
    "node": "22.x"
  }
}
